---
"agentmove-cli": minor
---

Agent Skills support for Cline (`~/.cline/skills/`, project `.cline/skills/`) and Warp (`~/.warp/skills/`, project `.warp/skills/`): skills now migrate natively in both directions instead of being skipped with a warning.
