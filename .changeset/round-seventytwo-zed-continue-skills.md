---
"agentmove-cli": minor
---

Agent Skills support for Zed (`~/.agents/skills/`, project `.agents/skills/`) and Continue (`~/.continue/skills/`, project `.continue/skills/`): skills now migrate natively in both directions instead of being skipped with a warning.
