---
"agentmove-cli": minor
---

Vibe Code CLI (Mistral) adapter (35th client): `~/.vibe/config.toml` `[[mcp_servers]]` array-of-table entries with explicit `transport` (stdio `command`/`args`/`env`, remote `url`/`headers`; other config keys preserved on rewrite; `api_key_env`/`api_key_header`/`api_key_format`, `startup_timeout_sec`/`tool_timeout_sec`, and `enabled_tools`/`disabled_tools` reported as client-specific), `~/.vibe/AGENTS.md` global instructions (persona approximated), and `~/.vibe/skills/` Agent Skills. Project scope covers `.vibe/config.toml`, root `AGENTS.md`, and `.vibe/skills/`.
