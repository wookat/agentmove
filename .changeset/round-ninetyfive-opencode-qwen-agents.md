---
"agentmove-cli": minor
---

Custom agents (subagents) support for OpenCode and Qwen Code: user scope `~/.config/opencode/agents/` (legacy `agent/` also read) and `~/.qwen/agents/`, project scope `.opencode/agents/` and `.qwen/agents/`. Markdown and YAML frontmatter are copied byte-faithfully with honest client-specific-field warnings.
