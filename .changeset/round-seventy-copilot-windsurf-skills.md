---
"agentmove-cli": minor
---

Agent Skills support for GitHub Copilot CLI (`~/.copilot/skills/`, project `.github/skills/`) and Windsurf (`~/.codeium/windsurf/skills/`, project `.windsurf/skills/`): skills now migrate natively in both directions instead of being skipped with a warning.
